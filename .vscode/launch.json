{
    "configurations": [
        {
            "name": "Run VideoTools",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}"
        }
    ]
}